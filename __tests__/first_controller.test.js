describe('First Controller', () => {
    it('should return 200 and the expected response for GET /endpoint', async () => {
        //const response = await request(app).get('/');
        expect(200).toBe(200);
    });
});